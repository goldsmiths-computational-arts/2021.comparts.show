
export default class ArtistModel {
    name;
    preferredName;
    slug;
    constructor(name, preferredName) {
        this.name = name;
        this.preferredName = preferredName;
        // this.slug = slug;
    }
}