export default class G05ScheduleModel {
  title
  artist
  time
  date

  constructor(title, artist, time, date) {
    this.title = title
    this.artist = artist
    this.time = time
    this.date = date
  }
}
